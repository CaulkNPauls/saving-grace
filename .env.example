# Postgres (Neon, via the Vercel Postgres marketplace integration)
DATABASE_URL=

# Vercel Blob (image storage)
BLOB_READ_WRITE_TOKEN=

# Admin auth — never commit real values.
# ADMIN_PASSWORD_HASH is a bcrypt hash, not the plaintext password.
ADMIN_USERNAME=
ADMIN_PASSWORD_HASH=
AUTH_SECRET=
